"use strict"//modo estrito ajuda a ter um código mais limpo, e variáveis não declaradas não são consideradas

let nome = 'Bruno';
console.log(nome); //pode imprimir a variável chamada pelo nome
console.log('CFB Cursos'); //pode imprimir a string
console.log('Nome: ' + nome); //pode imprimir concatenado(string com a variável)
